name: "frag"

services:
  node:
    container_name: "frag-node"
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/workspace
      - frag-node_modules:/workspace/node_modules
    tty: true

volumes:
  frag-node_modules: